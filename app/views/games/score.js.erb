// 072 Used the following to confirm the variables were being passed.
// alert('hello world!');
// alert('<%= @time_start %>');
// alert('<%= @time_end %>');
// alert('<%= @user_word %>');
// alert('<%= @user_letters %>');
// alert('<%= @grid_letters %>');
// alert('<%= @included %>');
// alert('<%= @valid_word %>');
// alert('<%= @result %>');

// 073 I just want to show the result as a modal so I'm going to see if I can append a modal.
  // 073a - At the following site there was a mention about 'passing local variables' but this wasn't necessary in this case. Not certain why...
  // http://guides.rubyonrails.org/layouts_and_rendering.html#using-partials
$("#gameboard").prepend("<%=j render 'score_modal' %>")
